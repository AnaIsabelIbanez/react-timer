import styled from 'styled-components';
import CustomListItem from '../../components/CustomListItem';

export default styled(CustomListItem)`
    && {
        border-top: 1px solid #EEE;
        padding: 18px 0 12px 0;
    }
`;
